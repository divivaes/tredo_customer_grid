## Employee Grid

```sh
$ git clone https://github.com/divivaes/tredo_customer_grid.git
$ cd tredo_customer_grid/
$ cp .env.example .env
$ composer install
$ npm install && npm run prod
$ php artisan key:generate --ansi
$ php artisan migrate
```

